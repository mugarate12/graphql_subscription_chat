type Query {
  loginUser(username: String!, password: String!): Token
  searchUser(username: String!): User
  
  messages(contact: String!): [Message]
  conversation(contactUsername: String!): [Message]
}
